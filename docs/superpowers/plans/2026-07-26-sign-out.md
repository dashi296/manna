# ログアウト導線の追加 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 自分のプロフィールページからログアウトできるようにする。

**Architecture:** `features/sign-out` スライスを新設し、`SignOutButton` が確認シート・処理中状態・遷移をすべて内包する。プロフィールページは自分を見ているときだけそのボタンを1つ置く。確認には既存の `Sheet`（`side="bottom"`）を使い、新規プリミティブは追加しない。

**Tech Stack:** TanStack Start / React 19 / Base UI (`@base-ui/react`) / Supabase Auth / Vitest + @testing-library/react

**Spec:** [`docs/superpowers/specs/2026-07-26-sign-out-design.md`](../specs/2026-07-26-sign-out-design.md) / Issue #72

## Global Constraints

- FSD のインポート方向を守る（pages → features → shared）。スライス内部は相対 import、外部からは `index.ts` 経由
- コメントは原則不要。WHY が自明でない場合のみ1行で記載する
- UI は TDD（失敗するテスト → 実装 → 通過）で実装する
- テストは `apps/pwa/tests/` 下に、`src/` の構造をミラーして置く
- テストコマンドは `apps/pwa` ディレクトリで `pnpm test <パス>`。全件はリポジトリルートで `pnpm test`
- ベースラインは **260 テスト**がパスしている状態
- コミットメッセージは英語。末尾に `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` を付ける

## File Structure

| ファイル | 役割 |
|---|---|
| `apps/pwa/src/features/sign-out/lib/redirectToLogin.ts` | 新規。ログイン画面への全体リロード。副作用を1関数に隔離してテストでモックできるようにする |
| `apps/pwa/src/features/sign-out/ui/SignOutButton.tsx` | 新規。ボタン＋確認シート＋処理中状態 |
| `apps/pwa/src/features/sign-out/index.ts` | 新規。`SignOutButton` を公開 |
| `apps/pwa/tests/features/sign-out/SignOutButton.test.tsx` | 新規。ボタン単体のテスト |
| `apps/pwa/src/pages/profile/$userId/index.tsx` | 変更（126-139行）。自分のときだけボタンを出す分岐を追加 |
| `apps/pwa/tests/pages/profile/index.test.tsx` | 変更。`loaderData` をテストごとに差し替えられる形にし、表示条件のテストを追加 |

**なぜ `redirectToLogin` を別ファイルにするか:** `window.location.href` への代入は jsdom では実行できず、テストから遷移の有無を検証できない。1関数に隔離してモックすれば「成功時は遷移する」「失敗時は遷移しない」を両方検証できる。失敗時に誤って遷移すると、ログイン済みのまま `/login` に飛ばされて即座に `/` へ戻される挙動になるため、ここは検証する価値がある。

---

### Task 1: SignOutButton（features/sign-out スライス）

**Files:**
- Create: `apps/pwa/src/features/sign-out/lib/redirectToLogin.ts`
- Create: `apps/pwa/src/features/sign-out/ui/SignOutButton.tsx`
- Create: `apps/pwa/src/features/sign-out/index.ts`
- Test: `apps/pwa/tests/features/sign-out/SignOutButton.test.tsx`

**Interfaces:**
- Consumes: `signOut(): Promise<void>`（`@/shared/lib/auth` に実装済み。失敗時は throw する）
- Produces: `export function SignOutButton(): JSX.Element`（props なし）。Task 2 がこれを `@/features/sign-out` から import する

- [ ] **Step 1: 失敗するテストを書く**

`apps/pwa/tests/features/sign-out/SignOutButton.test.tsx`:

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, within } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { SignOutButton } from '@/features/sign-out'

const mockSignOut = vi.fn()
const mockRedirectToLogin = vi.fn()

vi.mock('@/shared/lib/auth', () => ({
  signOut: () => mockSignOut(),
}))

vi.mock('@/features/sign-out/lib/redirectToLogin', () => ({
  redirectToLogin: () => mockRedirectToLogin(),
}))

const openSheet = async () => {
  await userEvent.click(screen.getByRole('button', { name: 'ログアウト' }))
  return screen.getByRole('dialog')
}

describe('SignOutButton', () => {
  beforeEach(() => {
    mockSignOut.mockReset().mockResolvedValue(undefined)
    mockRedirectToLogin.mockReset()
  })

  it('押すと確認シートを開く', async () => {
    render(<SignOutButton />)
    expect(screen.queryByRole('dialog')).toBeNull()

    const dialog = await openSheet()

    expect(within(dialog).getByText('ログアウトしますか？')).toBeInTheDocument()
  })

  it('キャンセルではログアウトしない', async () => {
    render(<SignOutButton />)
    const dialog = await openSheet()

    await userEvent.click(within(dialog).getByRole('button', { name: 'キャンセル' }))

    expect(mockSignOut).not.toHaveBeenCalled()
    expect(mockRedirectToLogin).not.toHaveBeenCalled()
  })

  it('確認するとログアウトしてログイン画面へ送る', async () => {
    render(<SignOutButton />)
    const dialog = await openSheet()

    await userEvent.click(within(dialog).getByRole('button', { name: 'ログアウトする' }))

    expect(mockSignOut).toHaveBeenCalledTimes(1)
    expect(mockRedirectToLogin).toHaveBeenCalledTimes(1)
  })

  it('失敗したら遷移せず、もう一度押せる状態に戻す', async () => {
    mockSignOut.mockRejectedValue(new Error('boom'))
    render(<SignOutButton />)
    const dialog = await openSheet()
    const confirm = within(dialog).getByRole('button', { name: 'ログアウトする' })

    await userEvent.click(confirm)

    expect(mockRedirectToLogin).not.toHaveBeenCalled()
    expect(confirm).not.toBeDisabled()
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd apps/pwa && pnpm test tests/features/sign-out/SignOutButton.test.tsx`
Expected: FAIL（`Failed to resolve import "@/features/sign-out"`）

- [ ] **Step 3: 遷移用のヘルパーを実装**

`apps/pwa/src/features/sign-out/lib/redirectToLogin.ts`:

```ts
// signOut が消すのはクライアント側のセッションだけで、SSR は cookie を読んで認証を
// 判定する（__root.tsx の beforeLoad）。クライアントルーティングだとサーバー側が
// 古い cookie を見る可能性があるため、全体リロードで確実にやり直す。
export function redirectToLogin() {
  window.location.href = '/login'
}
```

- [ ] **Step 4: ボタン本体を実装**

`apps/pwa/src/features/sign-out/ui/SignOutButton.tsx`:

```tsx
import { useState } from 'react'
import { Button } from '@/shared/ui/button'
import {
  Sheet,
  SheetClose,
  SheetContent,
  SheetDescription,
  SheetFooter,
  SheetHeader,
  SheetTitle,
} from '@/shared/ui/sheet'
import { signOut } from '@/shared/lib/auth'
import { redirectToLogin } from '../lib/redirectToLogin'

export function SignOutButton() {
  const [open, setOpen] = useState(false)
  const [pending, setPending] = useState(false)

  const confirm = async () => {
    if (pending) return
    setPending(true)
    try {
      await signOut()
      redirectToLogin()
    } catch {
      // 失敗時はシートを開いたまま、もう一度押せる状態に戻すだけにする
      setPending(false)
    }
  }

  return (
    <>
      <Button onClick={() => setOpen(true)} variant="outline" size="sm">
        ログアウト
      </Button>
      <Sheet open={open} onOpenChange={setOpen}>
        <SheetContent side="bottom" className="pb-6">
          <SheetHeader>
            <SheetTitle>ログアウトしますか？</SheetTitle>
            <SheetDescription>
              再度 Google でサインインすればまた利用できます
            </SheetDescription>
          </SheetHeader>
          <SheetFooter>
            <Button onClick={confirm} disabled={pending} variant="destructive">
              ログアウトする
            </Button>
            <SheetClose render={<Button variant="outline" />}>キャンセル</SheetClose>
          </SheetFooter>
        </SheetContent>
      </Sheet>
    </>
  )
}
```

成功時に `setPending(false)` を呼ばないのは意図的。遷移するので状態を戻す必要がない。

- [ ] **Step 5: スライスの公開 API を作る**

`apps/pwa/src/features/sign-out/index.ts`:

```ts
export { SignOutButton } from './ui/SignOutButton'
```

- [ ] **Step 6: テストが通ることを確認**

Run: `cd apps/pwa && pnpm test tests/features/sign-out/SignOutButton.test.tsx`
Expected: PASS（4 tests）

- [ ] **Step 7: 全件テストで回帰がないことを確認**

Run: リポジトリルートで `pnpm test`
Expected: 264 tests パス（ベースライン260 + 4）

- [ ] **Step 8: Commit**

```bash
git add apps/pwa/src/features/sign-out apps/pwa/tests/features/sign-out
git commit -m "feat: add a sign-out button with a confirmation sheet"
```

---

### Task 2: プロフィールページへの組み込み

**Files:**
- Modify: `apps/pwa/src/pages/profile/$userId/index.tsx:126-139`
- Test: `apps/pwa/tests/pages/profile/index.test.tsx`

**Interfaces:**
- Consumes: `SignOutButton`（Task 1、`@/features/sign-out` から import、props なし）
- Produces: なし（この計画の最終成果）

現在このファイルの126-139行は、他人のプロフィールのときだけボタンを出している。自分のときは何も出ていない。

```tsx
{currentUserId && currentUserId !== profile.id && (
  <div className="flex gap-2 mt-3">
    <FollowButton ... />
    <FamilyButton ... />
  </div>
)}
```

- [ ] **Step 1: 既存テストを loaderData 差し替え可能な形にする**

`apps/pwa/tests/pages/profile/index.test.tsx` の冒頭から `describe('ProfilePage', () => {` の直前まで（1-17行）を次に置き換える。既存3件のテスト本体は変更しない。

`@/shared/lib/supabase` のモックを足すのは、`currentUserId` を非 null にすると他人のプロフィールで `FollowButton` / `FamilyButton` が描画され、それらが supabase クライアントを import するため。現在は `currentUserId: null` 固定でこれらが描画されず、モックが不要だった。

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import { routeComponent } from '../../helpers/tanstack'

const loaderData = vi.fn()

vi.mock('@tanstack/react-router', async () =>
  (await import('../../helpers/tanstack')).routerMock(() => loaderData()),
)

vi.mock('@tanstack/react-start', async () => (await import('../../helpers/tanstack')).startMock())

vi.mock('@/shared/lib/supabase', () => ({
  supabase: { from: () => ({ insert: vi.fn(), delete: vi.fn() }) },
}))

const base = {
  profile: { id: 'u2', display_name: 'テスト太郎', avatar_url: null, bio: null },
  posts: [],
  currentUserId: null as string | null,
  isFollowing: false,
  familyStatus: 'none',
  followerCount: 3,
  followingCount: 5,
}
```

続く `describe('ProfilePage', () => {` の直後に次を追加する。

```tsx
  beforeEach(() => {
    loaderData.mockReturnValue(base)
  })
```

- [ ] **Step 2: 表示条件の失敗するテストを追加**

同ファイルの `describe` の末尾に2件追加する。

```tsx
  it('自分のプロフィールではログアウトボタンを表示する', async () => {
    loaderData.mockReturnValue({ ...base, currentUserId: 'u2' })
    const ProfilePage = routeComponent(await import('@/pages/profile/$userId/index'))
    render(<ProfilePage />)
    expect(screen.getByRole('button', { name: 'ログアウト' })).toBeInTheDocument()
  })

  it('他人のプロフィールにはログアウトボタンを表示しない', async () => {
    loaderData.mockReturnValue({ ...base, currentUserId: 'me' })
    const ProfilePage = routeComponent(await import('@/pages/profile/$userId/index'))
    render(<ProfilePage />)
    expect(screen.queryByRole('button', { name: 'ログアウト' })).toBeNull()
  })
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `cd apps/pwa && pnpm test tests/pages/profile/index.test.tsx`
Expected: 「自分のプロフィールでは〜」が FAIL（ログアウトボタンが存在しない）。既存3件と「他人の〜」は PASS

- [ ] **Step 4: プロフィールページに分岐を追加**

`apps/pwa/src/pages/profile/$userId/index.tsx` の import に1行足す。

```tsx
import { SignOutButton } from '@/features/sign-out'
```

126-139行の閉じ括弧 `)}` の直後に、自分のとき用の分岐を追加する。

```tsx
        {currentUserId === profile.id && (
          <div className="flex gap-2 mt-3">
            <SignOutButton />
          </div>
        )}
```

- [ ] **Step 5: テストが通ることを確認**

Run: `cd apps/pwa && pnpm test tests/pages/profile/index.test.tsx`
Expected: PASS（5 tests）

- [ ] **Step 6: 全件テストとビルド**

Run: リポジトリルートで `pnpm test`、続けて `cd apps/pwa && pnpm build`
Expected: 266 tests パス（264 + 2）、ビルド成功

- [ ] **Step 7: 実機で動作確認**

`/verify` スキル（Supabase ローカル + Vite dev + Playwright MCP）で以下を確認する。ローカル DB にユーザーが存在しない場合は Google でサインインして作る。

1. 自分のプロフィール（`/profile`）にログアウトボタンが表示される
2. 他人のプロフィールには表示されない（フォロー／ファミリーのボタンだけが出る）
3. ボタンを押すと確認シートが下から出る
4. キャンセルするとシートが閉じ、ログイン状態が保たれる
5. 「ログアウトする」を押すと `/login` に遷移する
6. 遷移後にブラウザバックしても認証ガードで `/login` に戻される（セッションが実際に消えている）

- [ ] **Step 8: Commit**

```bash
git add apps/pwa/src/pages/profile apps/pwa/tests/pages/profile
git commit -m "feat: show the sign-out button on your own profile"
```

---

## 完了後

`gh pr create` で PR を作成し、本文に `Closes #72` を含める。
